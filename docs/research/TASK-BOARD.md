# بورد قدیمی — منسوخ برای تخصیص کار

**از 2026-08-22 ایجنت حق ندارد از این فایل تسک بردارد.**

تخصیص فقط از طریق `MGT` است:

- زنده: [`docs/units/MGT/ISSUED.md`](../units/MGT/ISSUED.md)
- خانه واحدها: [`docs/units/README.md`](../units/README.md)

این فایل فقط بایگانی ایدهٔ موج اول است. ردیف‌های `R1-RND-*` و `R1-CUS-*` به صف‌های متوالی `RND` و `TRADE` منتقل شده‌اند.

بازرسی کالای فیزیکی دیگر کد `QC` نیست؛ کد `INSP` است. کنترل کیفیت ایجنت کد `QA` است.
