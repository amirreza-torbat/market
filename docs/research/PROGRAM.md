# برنامه تحقیق

اجرای روزبه‌روز دیگر اینجا نیست. اجرا:

- [`docs/units/MGT/`](../units/MGT/README.md)
- [`docs/units/QA/LOOP.md`](../units/QA/LOOP.md)
- [`docs/units/RND/QUEUE.md`](../units/RND/QUEUE.md)
- [`docs/units/TRADE/QUEUE.md`](../units/TRADE/QUEUE.md)

## چرا تحقیق قبل از ساخت؟

بدون دادهٔ رقیب و قانون صادرات، سایت پوسته‌ای از علی‌بابا می‌شود که در گمرک و پول می‌ترکد.

## اصول اجرایی فعلی

- داخل هر واحد تسک‌ها یکی‌پس‌ازدیگری
- `RND` و `TRADE` نسبت به هم موازی‌اند
- قبول فقط با `QA`
- واحدهای محصول از بسته `packages/` تغذیه می‌شوند نه از حافظه ایجنت

## خروج از تحقیق به طراحی

- `RND-009` و `TRD-014` در `qa-pass`
- ماتریس اسناد حداقل ۳ دسته × ۵ مقصد
- حداقل یک مسیر پرداخت در صف بعدی `FIN` که `LEG`+`CMP` تأیید کنند
- `PM` سند فرایند بومی را بعد از این بسته‌ها می‌نویسد
