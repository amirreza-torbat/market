# پرامپت اجرایی تسک TRD-005

**این متن را کامل به ایجنت مسئول (`TRADE+RND`) بده.**

## هویت ایجنت

پیش از اجرا، هویت [`TRADE`](../identities/TRADE.md) و [پروتکل عمومی](../00-GLOBAL-PROTOCOL.md) را بپذیر و با همین نقش پاسخ بده. تو فقط مجری همین تسک هستی؛ تسک بعدی را باز نکن و نتیجه را حدس نزن.

## مأموریت

شناسه: `TRD-005`  
مالک: `TRADE+RND`  
اولویت: `P0`  
وابستگی‌ها: `RND-001;TRD-002`  
خروجی مورد انتظار: `5-year destination x HS ranking`  
معیار پذیرش: Every rank has reproducible official-source calculation

## دستور دقیق

با اولویت پایگاه و گزارش رسمی گمرک ایران، آمار صادرات پنج سال اخیر را برای هر کد تعرفه و کشور مقصد استخراج کن. ارزش، وزن/مقدار، رشد سالانه، CAGR، سهم، تعداد سال رشد و آخرین سال را محاسبه کن. HS، سال پایه، واحد پول، داده مفقود و تغییر طبقه‌بندی را کنترل کن. کشور هدف را با حداقل داده، روند صعودی پایدار، اندازه بازار و حساسیت رتبه‌بندی رتبه‌بندی کن؛ هیچ کشور یا عددی را از حدس اضافه نکن. خروجی باید CSV خام، CSV محاسبه‌شده، فرمول و جدول رتبه‌بندی باشد.

## الزامات روش و منبع

- پیش از کار scope، تعریف اصطلاحات، query log و فهرست منابع را ثبت کن.
- منبع رسمی/اولیه اولویت دارد: گمرک ایران و مرجع رسمی کشور مقصد برای تجارت؛ قانون/رگولاتور رسمی برای حقوق و انطباق؛ اسناد رسمی ارائه‌دهنده برای پرداخت؛ مشاهده مستقیم برای UI. منبع ثانویه فقط سرنخ است.
- برای هر عدد: شاخص، مقدار، واحد، ارز، سال/تاریخ، HS یا طبقه‌بندی، کشور، URL کامل، نهاد، عنوان جدول و تاریخ دسترسی را بنویس.
- داده خام را جدا از داده محاسبه‌شده نگه دار و محاسبه را با اسکریپت تکرارپذیر انجام بده؛ گردکردن فقط در نمایش.
- هر مورد ناممکن را «مشاهده نشد/داده در دسترس نبود» بنویس و علت و روش بعدی را ذکر کن.
- ۱۰٪ رکوردها، همه اعداد رتبه اول و همه outlierها را دوباره کنترل کن؛ مغایرت‌ها را در reconciliation log نگه دار.

## قالب خروجی

گزارش فارسی یا انگلیسی دقیق، شامل: خلاصه اجرایی، scope، روش، جدول منابع، یافته‌های شماره‌دار، جداول داده، محاسبات/فرمول، محدودیت‌ها، ریسک‌ها، پیشنهادهای قابل‌اقدام، پرسش‌های باز، self-check و بسته handoff برای مصرف‌کننده بعدی. هیچ claim بدون citation باقی نماند. اگر خروجی داده‌ای است، CSV خام و CSV پردازش‌شده را کنار گزارش ذخیره کن.

## آدرس ذخیره‌سازی خودکار — اجباری

در انتهای اجرا، فایل اصلی را دقیقاً در این مسیر ذخیره کن:

`docs/units/TRADE/outbox/TRD-005.md`

اگر تسک چندمالک دارد، مالک ثبت‌کننده همان مالک اول (`TRADE`) است و بسته‌های مصرف‌کنندگان را در `docs/units/TRADE/packages/TRD-005-to-<UNIT>.md` قرار بده. برای فایل‌های داده‌ای از پوشه هم‌نام `TRD-005` در کنار گزارش استفاده کن. سپس طبق پروتکل مشترک، فایل را بازخوانی، activity log و notify را ایجاد کن و متوقف شو.
