# رأی QA — FIN-002 revision 1

- داور: نقش `QA` (بازرس مستقل کیفیت پژوهش) در نشست `arena/01a034c3-market`
- نویسنده گزارش: نقش `FIN` (مهندس مالی و معمار پرداخت تجارت)
- تفکیک نویسنده/داور: **yes** (نویسنده `FIN` روی شاخه `arena/01a034a7-market`؛ داور `QA` روی شاخه `arena/01a034c3-market`)
- تاریخ: 2026-08-24
- رأی: **pass** (با ۵ نیت غیرمسدود `accepted-nit`؛ تأیید نهایی/ثبت توسط `MGT`)

---

## ۱. مشخصات تسک و نویسنده

| فیلد | مقدار |
| --- | --- |
| شناسه تسک | `FIN-002` |
| مالک / نویسنده | `FIN` |
| اولویت | `P0` |
| وابستگی‌ها | `FIN-001; LEG-003` |
| خروجی مورد انتظار | hold-release-refund flow |
| معیار پذیرش | Inspection delivery dispute conditions mapped |
| وضعیت اعلام‌شده در NOTIFY | `in-qa` |

## ۲. شاخه منبع و commit منبع

| فیلد | مقدار |
| --- | --- |
| شاخه منبع (داوری‌شده، read-only) | `origin/arena/01a034a7-market` |
| commit منبع | `53b3b944d0f8008429bb62166b4d57a8ce9d6c2d` |
| canonical docs (read-only) | `origin/arena/01a029c7-market @ d805abd2151da480ffb21ebe1a6f516c332aa7b0` |
| شاخه داور | `arena/01a034c3-market` (`ab698e2` + این داوری) |

## ۳. روش داوری

1. کنترل شاخه/origin (مخزن `amirreza-torbat/market` ✓). تأیید عدم checkout/ویرایش شاخه منبع.
2. خواندن پرامپت `FIN-002`، هویت `QA`، `RUBRIC`/`JOB`/`LOOP` و `00-GLOBAL-PROTOCOL` از canonical.
3. استخراج read-only همه فایل‌های `FIN-002` از `origin/arena/01a034a7-market` (با `git show`، بدون fetch/checkout).
4. **بازتولید:** اجرای `build_fsm_data.py` و `validate_fsm.py` در کپی موقت؛ مقایسه بایت‌به‌بایت با فایل‌های commit‌شده.
5. بازشماری مستقل ۱۳ وضعیت، ۲۳ گذار، ۲۶ گیت، ۱۹ رویداد دفتری، K1–K7 مقابل CSV و map شرایط.
6. راستی‌آزمایی انطباق (PR-01..PR-07، تعلیق امارات، گیت‌های G2/G3/G4/G25/G26، مسیر ممنوع فقط CANCELLED/EXPIRED→REFUND).
7. راستی‌آزمایی وابستگی `LEG-003` (placeholder، نه متن تصویب‌شده) و تطبیق ادعای پیش‌شرط (QA-PASS-FIN-001).
8. تطبیق اعداد NOTIFY/report با commit message.

## ۴. فهرست فایل‌های بررسی‌شده (همگی read-only روی شاخه منبع)

| فایل | وضعیت |
| --- | --- |
| `docs/units/FIN/outbox/FIN-002.md` | read-only ✓ |
| `docs/units/FIN/FIN-002/state_machine.csv` | read-only ✓ (بازتولید یکسان) |
| `docs/units/FIN/FIN-002/transitions.csv` | read-only ✓ (بازتولید یکسان) |
| `docs/units/FIN/FIN-002/ledger_events.csv` | read-only ✓ (بازتولید یکسان) |
| `docs/units/FIN/FIN-002/constraints.csv` | read-only ✓ (بازتولید یکسان) |
| `docs/units/FIN/FIN-002/machine_summary.csv` | read-only ✓ (بازتولید یکسان) |
| `docs/units/FIN/FIN-002/state_machine_diagram.md` | read-only ✓ (بازتولید یکسان) |
| `docs/units/FIN/FIN-002/build_fsm_data.py` | read-only ✓ (اجرای مجدد موفق) |
| `docs/units/FIN/FIN-002/validate_fsm.py` | read-only ✓ (اجرای مجدد موفق، C1..C9 پاس) |
| `docs/units/FIN/packages/FIN-002-to-{MGT,PM,TRADE,INSP,LEG,CMP,INS}.md` | read-only ✓ |
| `docs/units/MGT/inbox/NOTIFY-FIN-002.md` | read-only ✓ |

## ۵. جدول بررسی (بند به بند)

| # | بند | نتیجه | شاهد |
| --- | --- | --- | --- |
| ۱ | دامنه فقط `FIN-002`؛ `FIN-003` شروع نشده | ✓ | بخش ۲ گزارش؛ NOTIFY؛ ACTIVITY-LOG `FIN` |
| ۲ | معیار پذیرش «بازرسی/تحویل/اختلاف mapped» | ✓ | بخش ۶.۴؛ هر وضعیت در §۵ trigger/evidence/actor/owner دارد |
| ۳ | ۱۱ وضعیت خواسته‌شده + `CANCELLED`/`EXPIRED` = ۱۳ | ✓ | `REQ_STATES` در `validate_fsm.py` = ۱۱؛ داده ۱۳ |
| ۴ | ۱۶ ویژگی اجباری هر وضعیت | ✓ | C2 پاس؛ 13×16 پر (بازشماری) |
| ۵ | ۲۳ گذار با guard/actor/trigger | ✓ | `transitions.csv`؛ بازتولید یکسان |
| ۶ | ۱۹ رویداد دفتری دوبل (+owner=FIN-003) | ✓ | C8؛ تطبیق debit/credit؛ بازتولید یکسان |
| ۷ | ۲۶ گیت انطباق؛ هیچ گیت دِنگلینگ | ✓ | C7؛ 26 تعریف‌شده = 26 ارجاع‌شده |
| ۸ | K1–K7 تعریف و ارجاع | ✓ | refund_condition هر وضعیت |
| ۹ | بازتولید `build_fsm_data.py` | ✓ | ۴ CSV بایت‌به‌بایت یکسان |
| ۱۰ | بازتولید `validate_fsm.py` | ✓ | C1..C9 پاس؛ `machine_summary.csv`/`diagram` بایت‌به‌بایت یکسان |
| ۱۱ | انطباق: PR-01..PR-07 پیاده نشده | ✓ | هیچ وضعیت/گذار مسیر PR؛ G3/G26 |
| ۱۲ | انطباق: تعلیق امارات مسدود (G2/G16/G26). بدون کارaround | ✓ | G2 applies_to AUTHORIZE/VERIFY/SHIP/DELIVERY؛ عدم وجود گذار امارات |
| ۱۳ | مسیر ممنوع فقط CANCELLED→REFUND | ✓ | C5 پاس؛ CANCELLED/EXPIRED → REFUND |
| ۱۴ | گیت‌های G2/G3/G4/G25/G26 موجود | ✓ | `constraints.csv`؛ بازتولید یکسان |
| ۱۵ | وابستگی `LEG-003` = placeholder (نه متن اختلاقی) | ✓ | §۵.۷/§۵.۱۱/§۶.۵ پرسش به LEG؛ بخش ۹ (در دسترس نبود) |
| ۱۶ | نمونه‌برداری ۱۰٪ + همه گیت‌ها + پایانه‌ها + outlier | ✓ | بخش ۷ این رأی (reconciliation) |
| ۱۷ | تطبیق اعداد NOTIFY/report با commit (13/23/19/26/7) | ✓ | commit `53b3b94`؛ NOTIFY؛ گزارش §۱ |
| ۱۸ | self-check نویسنده | ✓ | بخش ۱۳ گزارش |
| ۱۹ | محدودیت/پرسش باز/ریسک/handoff | ✓ | بخش‌های ۹–۱۲، ۱۴ |
| ۲۰ | فایل در مسیر اجباری | ✓ | `docs/units/FIN/outbox/FIN-002.md` |

## ۶. بازتولید (Reproducibility)

```text
python3 build_fsm_data.py
  wrote state_machine.csv: 13 rows
  wrote transitions.csv: 23 rows
  wrote ledger_events.csv: 19 rows
  wrote constraints.csv: 26 rows     EXIT 0

python3 validate_fsm.py
  states=13 transitions=23 ledgers=19 gates=26
  ALL CHECKS PASSED (C1..C9)
  wrote machine_summary.csv, state_machine_diagram.md   EXIT 0

cmp (byte-by-byte) -> state_machine.csv IDENTICAL
cmp -> transitions.csv IDENTICAL
cmp -> ledger_events.csv IDENTICAL
cmp -> constraints.csv IDENTICAL
cmp -> machine_summary.csv IDENTICAL
cmp -> state_machine_diagram.md IDENTICAL
```

**نتیجه بازتولید: تفاوت صفر در هر ۶ خروجی. مسیر داده → اعتبارسنجی → خلاصه/نمودار پایدار و تکرارپذیر است.**

## ۷. Reconciliation Log (نمونه‌برداری)

### ۷.۱ وضعیت‌ها (10٪ = ۲؛ + پایانه‌ها + outlierها)
| رکورد | کنترل | نتیجه |
| --- | --- | --- |
| HOLD | 16/16 ویژگی پر؛ gates=G5,G6,G7؛ ledger=L02؛ out=VERIFY,EXPIRED | تأیید ✓ |
| RESOLUTION | 16/16؛ term=1؛ gates=G24,G25؛ ledger=L16,L17,L18؛ out=RELEASE,REFUND | تأیید ✓ |
| RELEASE | 16/16؛ term=1؛ no-out؛ gates=G1,G18,G19,G20؛ ledger=L08,L10,L12 | تأیید ✓ |
| REFUND | 16/16؛ term=1؛ no-out؛ gates=G21,G22؛ ledger=L09,L13 | تأیید ✓ (به نیت n2 توجه شود) |
| CANCELLED | 16/16؛ out=REFUND؛ gates=G26؛ ledger=L19 | تأیید ✓ |
| EXPIRED | 16/16؛ out=REFUND؛ gates=G26؛ ledger=L19 | تأیید ✓ |

### ۷.۲ گذارها (10٪ = ۳؛ + نمونه‌های حیاتی)
| از→به | در csv؟ | guard |
| --- | --- | --- |
| AUTHORIZE→HOLD | ✓ | روش مجاز؛ سندبازار اولیه؛ مقصد فعال |
| HOLD→VERIFY | ✓ | تأیید LEG/CMP مدل نگهداشت؛ حساب امانی |
| DISPUTE→RESOLUTION | ✓ | شرط داوری LEG-005 |
| RESOLUTION→RELEASE / REFUND | ✓ | رأی کتبی؛ سندبازار نهایی / رأی کتبی |
| CANCELLED→REFUND / EXPIRED→REFUND | ✓ | کد علت (K1/K2/K7) / K7 |

### ۷.۳ گیت‌ها، رویدادها، K
- **۲۶ گیت:** همه ارجاع‌شده توسط وضعیت‌ها در `constraints.csv` وجود دارند (C7؛ بدون دِنگلینگ).
- **۱۹ رویداد:** همه ارجاع‌شده در `ledger_events.csv` وجود دارند (C8). دوبل‌نویسی برای L02/L08/L09/L15/L17/L18 معتبر است.
- **K1..K7:** همگی در `refund_condition` ارجاع و تعریف شده‌اند.

## ۸. انطباق و وابستگی‌ها

- **PR-01..PR-07:** هیچ state/transition مسیر غیرقانونی را پیاده نمی‌کند؛ G3 (prohibited) و G26 (بستن بدون workaround) ثبت شده‌اند.
- **تعلیق امارات (TRD-006):** G2 با `applies_to=AUTHORIZE,VERIFY,SHIP,DELIVERY` مقصد امارات را مسدود مطلق می‌کند؛ هیچ گذاری برای «تحویل/تسویه امارات» وجود ندارد؛ مسیر فقط CANCELLED→REFUND.
- **G4/G25/G26 (ممنوعیت دور زدن):** حاضر؛ رفتار بلوک + گزارش.
- **LEG-003:** جزئیات داوری/پذیرش ضمنی/آستانه‌ها در گزارش **placeholder/dependency** است و صریحاً «در دسترس نبود؛ نیازمند خروجی LEG» ثبت شده (بخش ۹). متن اختلاقی خلق نشده.
- **مدل نگهداشت وجه:** گیت G5 مسدودکننده؛ نیازمند تأیید کتبی LEG/CMP — درست ثبت شده.

## ۹. جدول همه نقص‌ها

| # | severity | code | file | محل | ادعا/فیلد | مشاهده‌شده | مورد انتظار | شاهد/اثر | وضعیت |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| n1 | m | m-meta (فرایندی) | `FIN-002.md` | پیش‌شرط (بالای بخش ۱) | «QA-PASS-FIN-001.md روی هیچ ref محلی/ریموتی یافت نشد» | فایل روی remote در `origin/arena/01a034c3-market @ ab698e2` موجود است (MGT تأیید کرده) | اعلام وجود/ارجاع به مسیر | رفع، ثبت واقعیت؛ خودِ اعتراف صادقانه است ولی پوشش ref ناقص | `accepted-nit` |
| n2 | m | m-struct (ناسازگاری توصیفی داخلی) | `FIN-002.md` | §5.9 REFUND (transition) | «در REFUND جزئی → RESOLUTION یا RELEASE» | `REFUND` پایانه است (out=[]، C4، machine_summary)، و تسویه جزئی در `RESOLUTION→RELEASE+REFUND` مدل شده | هم‌راستایی متن با داده | صدق بر مدل داده (معتبر/درست)؛ خطای متن در یک سلول توصیفی | `accepted-nit` (هم‌مرز M؛ تصمیم نهایی MGT) |
| n3 | m | m-meta/m-typo | `build_fsm_data.py` | docstring | «transitions.csv (21 transitions...)» | ۲۳ گذار نوشته می‌شود | ۲۳ در docstring | فقط کامنت؛ بدون اثر بر خروجی | `accepted-nit` |
| n4 | m | m-struct | `FIN-002.md` | §6.4 نقشه شرایط | ستون actor/owner | در جدول صراحتاً نیست؛ ولی در §۵.x (actor + legal_dependency) موجود است | اعم صریح‌سازی | پوشش در سطح وضعیت‌ها موجود است | `accepted-nit` |
| n5 | m | m-meta (برچسب مسیر خطا) | `FIN-002.md` | §5.8 RELEASE (transition) | «شکست → DISPUTE» بدون یال داده | RELEASE پایانه؛ هیچ یال RELEASE→DISPUTE در csv نیست؛ مسیر خطا فقط *نیمه‌بازدارنده* است (وجه می‌ماند) | ثبت‌شدن به‌عنوان failure-path نه یال | معنای failure-path؛ بدون تغییر مسیر واقعی | `accepted-nit` |

**شمارش: C = ۰، M = ۰، m = ۵ (همگی `accepted-nit`؛ غیرمسدود).**

## ۱۰. رأی نهایی

**pass**

- C = 0
- M = 0
- m = 5 (همگی `accepted-nit`؛ تأیید نهایی/ثبت با `MGT`)

## ۱۱. شرایط PASS

1. **بازتولید‌پذیری:** `build_fsm_data.py` و `validate_fsm.py` هر ۶ خروجی را بایت‌به‌بایت بازتولید کردند؛ C1..C9 پاس.
2. **پوشش معیار پذیرش:** شرایط بازرسی/تحویل/اختلاف با trigger/evidence/actor/owner در سطح وضعیت‌ها map شده‌اند.
3. **کامل‌بودن:** ۱۳ وضعیت × ۱۶ ویژگی = ۲۰۸ سلول همگی غیرخالی؛ ۲۳ گذار؛ ۱۹ رویداد؛ ۲۶ گیت؛ K1–K7.
4. **انطباق:** هیچ state/transition مسیر PR یا دورزدن تعلیق امارات را پیاده نمی‌کند؛ مسیر ممنوع فقط CANCELLED/EXPIRED→REFUND.
5. **LEG-003:** به‌عنوان placeholder/dependency رعایت شده؛ متن حقوقی تصویب‌شده خلق نشده.

## ۱۲. دستور بعدی (بند ۱۱ LOOP)

- پس از pass، `MGT` مجاز به ابلاغ `FIN-003` است (طبق handoff؛ قبل از آن `LEG-003` و تأیید `LEG/CMP` برای مدل نگهداشت — G5).
- **نیت‌های فوق باید توسط `MGT` پذیرفته (`accepted-nit`) شوند.** اگر `MGT` بند n2 را «تناقض داخلی» مادی بداند و آن را M تلقی کند، رأی به `fail` تغییر می‌کند (داور ثبت کرد تا تصمیم transparent باشد).

## ۱۳. محدودیت‌ها و Open Questions داور

- دو نیت (n2، n5) «ناسازگاری توصیفی/برچسب مسیر خطا» در متن علی‌رغم صحت مدل داده است؛ بازبینی نهایی با MGT لازم است.
- اعتبارسنجی بر اساس env Python 3.11.2 انجام شد؛ وابستگی خارجی نداشت.
- داوری روی شاخه read-only و بدون fetch مستقیم برخی منابع ثانویه بود؛ محدودیت‌های گزارش (بخش ۹) پذیرفته شد.
- تأیید محتوای حقوقی (LEG/CMP) و نرخ‌ها (FIN-003) خارج از دامنه این داوری است.

## ۱۴. Self-check داور

| بند | بله/خیر |
| --- | --- |
| شاخه منبع read-only ماند | بله |
| گزارش را بازنویسی نکردم | بله |
| رأی خودکار ندادم؛ بازتولید/بازشماری مستقل انجام شد | بله |
| نقص‌ها با محل و شاهد ثبت شدند | بله |
| فایل‌های `docs/units/FIN/…` تغییر نکردند | بله |
| همه خروجی‌ها روی شاخه مجاز نشست ثبت شدند | بله |
| `FIN-003`/تسک دیگر شروع نشد | بله |
