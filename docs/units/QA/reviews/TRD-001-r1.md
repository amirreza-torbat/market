# ارزیابی مستقل کنترل کیفیت — TRD-001 (بازنگری ۱)

- **شناسه تسک:** `QA-TRD-001`
- **موضوع ارزیابی:** بررسی مستقل خروجی تسک `TRD-001` (نقشه چرخه اسناد تجاری بین‌الملل)
- **داور / ارزیاب:** بازرس ارشد و مستقل کیفیت پژوهش (`QA`)
- **واحد مجری:** `TRADE`
- **تاریخ ارزیابی:** 2026-08-24 UTC
- **شاخه منبع:** `arena/01a034a2-market`
- **کامیت منبع:** `7e62b616d83c8b80bc4f60845d3e76f89a61d617`
- **رأی نهایی:** `QA-PASS-TRD-001`

---

## عبارت الزامی شروع داوری

> من ایجنت مستقل QA و بازرس ارشد کیفیت پژوهش هستم.
> دامنه این اجرا فقط QA-TRD-001 و بررسی خروجی TRD-001 است.
> گزارش TRADE را فقط read-only بررسی می‌کنم.
> هر ادعا را با شاهد و معیار مشخص ارزیابی می‌کنم.
> پس از ثبت رأی متوقف می‌شوم و تسک دیگری (از جمله RND-003، TRD-002 یا TRD-003) را شروع نمی‌کنم.

---

## ۱. مشخصات تسک و نویسنده

- **تسک:** `TRD-001` — نقشه چرخه اسناد تجاری بین‌الملل (`document lifecycle map`)
- **واحد مالک:** `TRADE`
- **اولویت:** `P0`
- **وابستگی‌ها:** ندارد
- **معیار پذیرش مصوب:** `Issuer condition and order state for each document`
- **وضعیت اعلام‌شده توسط مجری:** `in-qa`

---

## ۲. شاخه منبع و کامیت منبع

- **مخزن:** `https://github.com/amirreza-torbat/market.git`
- **شاخه منبع بازبینی‌شده (read-only):** `arena/01a034a2-market`
- **کامیت بازبینی‌شده:** `7e62b616d83c8b80bc4f60845d3e76f89a61d617`
- **شاخه جاری QA:** `arena/01a034bd-market`

---

## ۳. روش داوری و ممیزی مستقل

داوری تسک `TRD-001` به‌صورت کاملاً مستقل، سخت‌گیرانه و بر اساس پروتکل عمومی (`00-GLOBAL-PROTOCOL.md`)، سند هویت (`01-AGENT-IDENTITY.md` و `identities/QA.md`) و چک‌لیست روبریک QA انجام شد:

1. **بررسی استقلال و عدم ویرایش منبع:** فایل‌های منبع TRADE صرفاً به‌صورت خواندنی (`read-only`) از شاخه منبع دریافت و بررسی شدند؛ هیچ فایل منبعی در پوشه `docs/units/TRADE/` ایجاد یا ویرایش نشد.
2. **ارزیابی تطابق با بریف و معیار پذیرش:** کنترل شد که برای تمام ۲۴ سند تجاری شناسایی‌شده، شرایط صادرکننده (`issuer condition`) و گیت وضعیت سفارش (`order state gate`) به‌صورت تفکیک‌شده و ساختاریافته تدوین شده باشد.
3. **اعتبارسنجی داده‌های جدولی و CSV:** ساختار و سازگاری داده‌های خام (`TRD-001-document-lifecycle-raw.csv`) و پردازش‌شده (`TRD-001-document-lifecycle-processed.csv`) از نظر تعداد سطرها (۲۴ رکورد)، ستون‌ها، شناسه‌ها (D01 تا D24)، اتصالات منابع (S01 تا S21) و انطباق با متن گزارش با اسکریپت پایتون کنترل شد.
4. **کنترل منابع و استنادها:** ۲۱ منبع ثبت‌شده (شامل مراجع سازمان تجارت جهانی، آنسیترال، اتاق بازرگانی بین‌المللی، گمرک، سازمان جهانی بهداشت حیوانات و سازمان حفظ نباتات) به همراه URL کامل و تاریخ دسترسی بررسی شدند.
5. **ارزیابی تفکیک واقعیت، عرف و پیشنهاد:** کنترل شد که قوانین رسمی، عرف تجاری، استنباط‌ها و پیشنهادات محصول با برچسب‌های تفکیک‌شده ارائه شده باشند و هیچ وضعیت پیشنهادی به‌عنوان حکم قانونی قطعی جا زده نشده باشد.
6. **نمونه‌برداری و بازآزمایی:** ۳ رکورد از ۲۴ رکورد (۱۲.۵٪ — بالاتر از آستانه ۱۰٪) شامل Commercial Invoice، Certificate of Origin و Bill of Lading به‌صورت مستقل با منابع ارجاع‌شده بازآزمایی شدند.
7. **کنترل مرزهای اجرایی:** بررسی شد که تسک‌های بعدی (`TRD-002`، `TRD-003`، `RND-003`) آغاز نشده باشند.

---

## ۴. فهرست فایل‌های بررسی‌شده

| ردیف | مسیر فایل | شاخه / منبع | وضعیت ممیزی |
| --- | --- | --- | --- |
| ۱ | `docs/units/TRADE/outbox/TRD-001.md` | `arena/01a034a2-market` (`7e62b61`) | خوانده شد — ۳۵۰ خط — کامل و منطبق بر روبریک |
| ۲ | `docs/units/TRADE/outbox/TRD-001/TRD-001-document-lifecycle-raw.csv` | `arena/01a034a2-market` (`7e62b61`) | خوانده شد — ۲۴ سطر داده + ۱۱ ستون — معتبر |
| ۳ | `docs/units/TRADE/outbox/TRD-001/TRD-001-document-lifecycle-processed.csv` | `arena/01a034a2-market` (`7e62b61`) | خوانده شد — ۲۴ سطر داده + ۸ ستون — معتبر |
| ۴ | `docs/units/TRADE/outbox/TRD-001/README.md` | `arena/01a034a2-market` (`7e62b61`) | خوانده شد — مستندسازی داده‌های همراه |
| ۵ | `docs/units/MGT/inbox/NOTIFY-TRD-001.md` | `arena/01a034a2-market` (`7e62b61`) | خوانده شد — اعلان رسمی وضعیت in-qa |
| ۶ | `docs/units/TRADE/ACTIVITY-LOG.md` | `arena/01a034a2-market` (`7e62b61`) | خوانده شد — ثبت رویداد در لاگ فعالیت واحد |
| ۷ | `docs/prompts/tasks/TRD-001.md` | `arena/01a029c7-market` | مرجع بریف و معیار پذیرش |
| ۸ | `docs/research/task-registry.csv` | `arena/01a029c7-market` | تطبیق شناسه و وابستگی‌ها |

---

## ۵. جدول ممیزی چک‌لیست ۱۴گانه اجباری

| ردیف | موضوع بررسی | شواهد و جزئیات بررسی در TRD-001 | نتیجه |
| --- | --- | --- | --- |
| ۱ | **پوشش کامل چرخه اسناد** | گزارش ۲۴ سند تجاری را در تمام فازهای تجاری شامل پیش‌قرارداد (RFQ، Quote، PI)، قرارداد (PO، Sales Contract)، بسته‌بندی و آماده‌سازی (Packing List)، گمرک و ترخیص (اظهارنامه گمرکی)، حمل بین‌المللی (Bill of Lading)، گواهی‌های مشروط (COO، مجوزها، SPS، Halal، بازرسی، ضدعفونی، بیمه)، تحویل (Delivery Note، POD، GRN، Release Order) و موارد استثنایی (Claim، Dispute) پوشش داده است. | **PASS** |
| ۲ | **یکتایی و تفکیک شناسه‌ها** | شناسه‌های D01 تا D24 برای اسناد، S01 تا S21 برای منابع، Q1 تا Q6 برای زیرسؤال‌ها و QL-01 تا QL-12 برای کوئری‌ها کاملاً یکتا، منظم و متقابل ارجاع شده‌اند. | **PASS** |
| ۳ | **صحت داده‌ها و تفکیک سطوح** | تفکیک دقیق بین `واقعیت-منبع`، `عرف-تجاری`، `استنباط`، `پیشنهاد-محصول` و `مجهول` در سراسر گزارش رعایت شده است. | **PASS** |
| ۴ | **تطابق داده خام و پردازش‌شده** | هر دو فایل CSV دارای ۲۴ ردیف متناظر دقیق D01 تا D24 هستند. ستون‌های `issuer_class`, `requiredness_default`, `lifecycle_phase`, `order_state_gate`, `platform_control` به‌درستی از فیلدهای تفصیلی خام نرمال شده‌اند. | **PASS** |
| ۵ | **اعتبارسنجی داده‌ها و ساختار** | فایل‌های CSV استاندارد، UTF-8 بدون BOM و بدون خطای نگارشی هستند؛ ساختار ستون‌ها و سطرهای داده بدون ناهماهنگی است. | **PASS** |
| ۶ | **صحت شرط صادرکننده و وضعیت سفارش** | برای تمام ۲۴ سند، فیلدهای `issuer_condition` و `order_state` / `order_state_gate` به‌طور شفاف و استاندارد تعیین شده و معیار پذیرش تسک را ۱۰۰٪ ارضا کرده است. | **PASS** |
| ۷ | **کامل‌بودن متادیتای منابع** | تمام ۲۱ منبع در جدول بخش ۵ دارای نام نهاد، عنوان، نوع، سطح اتکا، URL کامل، تاریخ انتشار/نسخه و یادداشت نحوه دسترسی هستند. | **PASS** |
| ۸ | **نمونه‌برداری و بازآزمایی مستقل** | نمونه‌برداری از ۳ سند کلیدی (Commercial Invoice، Certificate of Origin، Bill of Lading) معادل ۱۲.۵٪ کل اسناد (بیشتر از آستانه ۱۰٪) با منابع اصلی (ITA S01, ICC S04/S06/S07, IMO S08) تطبیق داده شد و صحت آن‌ها تأیید گردید. | **PASS** |
| ۹ | **بررسی اسناد کلیدی و حیاتی** | اسناد حساس مانند بارنامه دریایی (تفکیک received vs on-board و الزامات VGM)، گواهی مبدأ (انواع ترجیحی و غیرترجیحی و راستی‌آزمایی اتاق بازرگانی)، فاکتور تجاری در برابر پیش‌فاکتور، و اظهارنامه گمرکی بررسی و صحه‌گذاری شدند. | **PASS** |
| ۱۰ | **بررسی خودارزیابی نویسنده** | جدول خودارزیابی ۱۳ بندی در بخش ۱۶ گزارش توسط مجری به‌طور کامل و با ارجاع به بخش‌های گزارش تکمیل شده است. | **PASS** |
| ۱۱ | **عدم وجود ادعای بدون منبع و شفافیت مجهولات** | هیچ ادعای بدون شاهدی وجود ندارد. ناپایداری دسترسی برخط به سامانه‌های داخلی ایران (IRICA / EPL / NTSW) و عدم امکان عمومی‌سازی هزینه‌ها/زمان‌ها با برچسب `مجهول/داده در دسترس نبود` به همراه علت و روش جایگزین به‌صورت صادقانه ثبت شده است. | **PASS** |
| ۱۲ | **صحت مسیرها و نام‌گذاری فایل‌ها** | فایل‌ها دقیقاً در مسیرهای ابلاغ‌شده استاندارد (`docs/units/TRADE/outbox/TRD-001.md`, `docs/units/TRADE/outbox/TRD-001/`, `docs/units/MGT/inbox/NOTIFY-TRD-001.md`, `docs/units/TRADE/ACTIVITY-LOG.md`) قرار دارند. | **PASS** |
| ۱۳ | **فرمت لینک‌ها و نگارش Markdown** | تمام عناوین، جداول، بلوک‌های نقل‌قول و لینک‌ها استاندارد و معتبر هستند؛ هیچ تگ معیوب یا خطای قالب‌بندی وجود ندارد. | **PASS** |
| ۱۴ | **کیفیت Handoff و مرزبندی تسک** | بخش ۱۷ بسته انتقال به QA و MGT را به‌صورت دقیق تعریف کرده است؛ تسک‌های بعدی `TRD-002`, `TRD-003` و `RND-003` به‌درستی مسدود مانده و شروع نشده‌اند. | **PASS** |

---

## ۶. جدول شمارش و ارزیابی نقص‌ها

| لایه نقص | تعداد مجاز | تعداد مشاهده‌شده | وضعیت |
| --- | --- | --- | --- |
| **بحرانی (Critical — C)** | ۰ | ۰ | **تأیید شد (PASS)** |
| **عمده (Major — M)** | ۰ | ۰ | **تأیید شد (PASS)** |
| **جزئی شمارش‌پذیر (Minor — m)** | کمتر از ۱٪ (۰) | ۰ (۰٪) | **تأیید شد (PASS)** |

### مشاهدات غیرمسدودکننده (Non-countable Meta Observations):
1. **`m-env-fetch` (ثبت صادقانه محدودیت محیطی):** عدم پایداری دسترسی مستقیم محیط سندباکس به پورتال‌های داخلی (`irica.ir` و `tpo.ir` و `ntsw.ir`) به عنوان مجهول ثبت شده و روش حل بعدی (مشاهده لاگین‌شده توسط کارگزار مجاز) درج گردیده است که طبق پروتکل رفتار صحیح است.
2. **`m-scope-guard` (کنترل سخت‌گیرانه دامنه):** تعرفه‌های گمرکی خاص، احکام ممنوعیت کالایی و جداول هزینه/زمان به دلیل وابستگی به پرونده مقصد خارج از دامنه اعلام شده‌اند و به‌درستی از ساخت اعداد ساختگی پرهیز شده است.

---

## ۷. نتیجه اجرای اسکریپت و اعتبارسنجی داده‌ها

کد اعتبارسنجی مستقل پایتون برای ممیزی ساختاری فایل‌های داده اجرا شد:
- **تعداد رکوردهای فایل خام:** ۲۴ رکورد
- **تعداد رکوردهای فایل پردازش‌شده:** ۲۴ رکورد
- **ستون‌های فایل خام:** ۱۱ فیلد (`document_id`, `document_type`, `issuer`, `issuer_condition`, `recipient`, `stage`, `precondition`, `order_state`, `error_risks`, `source_ids`, `evidence_label`)
- **ستون‌های فایل پردازش‌شده:** ۸ فیلد (`document_id`, `document_type`, `issuer_class`, `requiredness_default`, `lifecycle_phase`, `order_state_gate`, `platform_control`, `source_ids`)
- **هم‌پوشانی شناسه‌ها:** تطابق ۱۰۰٪ شناسه‌های D01 تا D24
- **هم‌پوشانی منابع:** تمام ۲۱ منبع استفاده‌شده در CSV در جدول رجیستری منابع گزارش وجود دارند.

---

## ۸. نمونه‌برداری و بازآزمایی اسناد منتخب

| شناسه | نام سند | شرط صادرکننده و وضعیت سفارش | بررسی منبع و ارزیابی QA | نتیجه |
| --- | --- | --- | --- | --- |
| **D06** | Commercial Invoice | صادرکننده: فروشنده/صادرکننده واقعی معامله؛ شرط صدور: نهایی‌شدن داده‌های معامله؛ گیت: `READY_TO_SHIP` | تطبیق با راهنمای ITA S01 و قواعد Incoterms S04؛ تفکیک روشن از پیش‌فاکتور (PI). | **PASS** |
| **D08** | Certificate of Origin | صادرکننده: اتاق بازرگانی یا مرجع مجاز؛ شرط صدور: ارائه شواهد مبدأ و تطبیق محموله؛ گیت: `DOCUMENTS_PENDING` | تطبیق با رهنمودهای ICC S06/S07 و ITA S21؛ تفکیک فرم ترجیحی/غیرترجیحی و مکانیسم verification. | **PASS** |
| **D09** | Bill of Lading | صادرکننده: متصدی حمل (Carrier)؛ شرط صدور: دریافت/بارگیری کالا و ثبت VGM؛ گیت: `IN_TRANSIT` | تطبیق با استانداردهای IMO S08/S09؛ تفکیک received for shipment از on-board. | **PASS** |

---

## ۹. یافته‌های خارج از محدوده و مرز تسک

- هیچ‌گونه ورود غیرمجاز به حیطه تسک‌های `TRD-002` (ماتریس کالا × کشور × مجوز)، `TRD-003` (مسئولیت‌های لجستیکی و فورواردر)، `RND-003` (تحلیل پلتفرم‌ها) یا تسک‌های حقوقی/مالی مشاهده نشد.
- پیشنهادات محصولی با عنوان صریح «پیشنهاد-محصول» علامت‌گذاری شده و تصمیم‌گیری نهایی به واحدهای تخصصی ذی‌ربط (`CUS`, `LEG`, `FIN`, `ENG`) واگذار شده است.

---

## ۱۰. رأی نهایی

**رأی قطعی کنترل کیفیت:** **`QA-PASS-TRD-001`**

خروجی تسک `TRD-001` کلیه الزامات کیفی، معیارهای پذیرش، تفکیک داده‌ها، متادیتای منابع و استانداردهای روبریک سازمانی را با خطای صفر برآورده کرده است و برای ورود به منبع حقیقت پروژه تأیید می‌شود.

---

## ۱۱. محدودیت‌ها و پرسش‌های باز برای واحدهای بعدی

1. **واحد `TRADE / CUS`:** در تسک‌های بعدی (نظیر TRD-002) مسیر لاگین‌شده در سامانه‌های گمرکی ایران و الزامات هر کشور مقصد تدوین شود.
2. **واحد `FIN`:** الزامات ارائه اسناد تحت روش‌های پرداخت مختلف (`LC`, `CAD`, `TT`) و تطابق بانکی مشخص گردد.
3. **واحد `LEG`:** قوانین حاکم، شروط حل اختلاف و ساختار پرونده‌های ادعا (Claim) در قالب‌های قراردادی پیاده‌سازی شود.

---

## ۱۲. خودارزیابی داور QA

| بند کنترلی | وضعیت |
| --- | --- |
| هویت مستقل QA و عبارت شروع رعایت شد | بله |
| شاخه منبع و فایل‌های RND/TRADE صرفاً read-only ماندند | بله |
| تمام ۱۴ بند روبریک به‌صورت باینری و مستند ارزیابی شدند | بله |
| اسکریپت و ساختار داده‌ها مستقل اجرا و اعتبارسنجی شدند | بله |
| نمونه‌برداری بالاتر از ۱۰٪ انجام شد | بله (۱۲.۵٪) |
| تسک بعدی شروع نشد و پس از صدور رأی توقف کامل صورت می‌گیرد | بله |
