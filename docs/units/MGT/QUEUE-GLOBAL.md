# صف جهانی مدیریت

خطوط موازی مجاز: `RND` ∥ `TRADE` ∥ (de-facto: `FIN`، `LEG` — اجرای owner-directed؛ ثبت 2026-08-24)
داخل هر خط: سری. تسک بعد فقط با `qa-pass`.

جزئیات داخل:

- [`../RND/QUEUE.md`](../RND/QUEUE.md) (canonical: `01a029c7@d805abd`)
- [`../TRADE/QUEUE.md`](../TRADE/QUEUE.md) (canonical: `01a029c7@d805abd`)

## وضعیت زنده خطوط (2026-08-24 — سازگاری‌شده توسط MGT)

| خط | تسک جاری | in-qa | بعدی (مسدود) |
| --- | --- | --- | --- |
| RND | — (در انتظار رأی) | `RND-003` | `RND-004` |
| TRADE | `TRD-003` (issued، بدون outbox) | `TRD-004`، `TRD-005`، `TRD-006` | `TRD-007` (همچنین گیت امارات) |
| FIN | — (در انتظار ابلاغ) | — (`FIN-002` qa-pass ✓) | `LEG-003` → `FIN-003` (LEG-003 با LEG-001 مسدود) |
| LEG | — (در انتظار رأی) | `LEG-001` | `LEG-002` / `LEG-003` |
| CMP | — | — | `CMP-001` (آماده؛ در انتظار دستور صریح کارفرما) |
| QA | — | صف: `RND-003` → `LEG-001` → `TRD-004/005/006` (`FIN-002` ✓ تمام) | — |

واحدهای `PM` `DES` `SEO` `INS` `INSP` `BUY` `ENG` `SUP` `UX` `PIM` `MOB` `CS` `TNS`: صف اجرایی بسته است. بسته‌ها در `routed/` می‌مانند.

## قاعده مسیریابی (طبق §11 مأموریت MGT — 2026-08-24)

`MGT` فقط فایل‌های زیر `docs/units/MGT/` را تغییر می‌دهد؛ بنابراین **همه PACKها، حتی برای خطوط باز (RND/TRADE/FIN/LEG)، در `routed/<DEST>/` قرار می‌گیرند** و واحد مصرف‌کننده آن‌را از همان‌جا می‌خواند. این قاعده جایگزین بند «خط باز → inbox مقصد» در PLAYBOOK قدیمی می‌شود و در ACTIVITY-LOG ثبت شد.
