# پیش‌نویس ابلاغ

اینجا تسک بعد آماده است اما issued نیست. ایجنت RND/TRADE از این پوشه کار برندارد.

مدیر بعد از `QA-PASS` محتوا را به `docs/units/<UNIT>/inbox/` کپی می‌کند و وضعیت را `issued` می‌کند.
