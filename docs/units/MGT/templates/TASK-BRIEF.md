# قالب بریف تسک — فقط `MGT` صادر می‌کند

کپی کنید به `docs/units/<UNIT>/inbox/<TASK-ID>.md`

```md
# <TASK-ID> — <عنوان یک‌خطی>

- واحد مجری:
- صادرکننده: MGT
- تاریخ ابلاغ:
- وضعیت: issued
- پیش‌نیاز: <TASK-ID یا هیچ>
- خروجی فایل:
- سقف حلقه کیفیت این تسک: 3 برگشت (بعد از آن MGT دامنه را خرد می‌کند)

## هدف
یک جمله: بعد از این تسک چه چیزی حقیقت ریپو می‌شود؟

## خارج از دامنه
چه چیزی را عمداً نباید در این تسک نوشت؟

## حتماً بخوانید قبل از شروع
- docs/units/<UNIT>/ACTIVITY-LOG.md
- docs/units/<UNIT>/COVERED-TOPICS.md
- <فایل‌های مرتبط قبلی>

## کارهایی که نباید تکرار شوند
موضوعاتی که قبلاً done شده و در این تسک فقط ارجاع می‌دهید:

## روش اجباری
- حداقل تعداد منبع مستقل:
- حداقل موجودیت مورد بررسی (سایت / کشور / سند):
- برچسب واقعیت / فرض:

## ابعاد اجباری (از روبریک واحد)
لیست کد ابعاد. خالی گذاشتن هر کدام = رد QA.

## اثر روی واحدهای دیگر
چه بسته‌ای بعد از qa-pass باید در packages/ گذاشته شود و برای کی؟

## معیار قبول (خلاصه)
QA با روبریک واحد می‌سنجد. نقص بحرانی صفر است.

## HANDOFF بعد از قبول
- به:
- تصمیم مورد نیاز:
```
