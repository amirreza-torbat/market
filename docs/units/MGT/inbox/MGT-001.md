# MGT-001 — راه‌اندازی و نگهبانی میز ابلاغ

- واحد مجری: `MGT`
- صادرکننده: کارفرما / استقرار
- تاریخ: 2026-08-22
- وضعیت: issued
- خروجی شیفت: `docs/units/MGT/outbox/MGT-001.md`
- این تسک ایستاده است؛ با یک گزارش بسته نمی‌شود

## هدف

میز مدیریت زنده باشد: اطلاع پایان را بگیرد، بسته RND را به واحد مربوطه بفرستد، تسک بعد را فقط بعد از QA ابلاغ کند.

## کار همین شیفت

1. `PLAYBOOK.md` و `ROUTING.md` را مبنای عمل کن نه حافظه
2. `ISSUED.md` را با چهار خط زنده هم‌خوان نگه دار: MGT, QA, RND, TRADE
3. inbox را پردازش کن؛ اگر خالی است در outbox بنویس خالی
4. به محض `RND-001-to-TRADE` آن را طبق ROUTING بفرست بدون اینکه CURRENT تجارت را عوض کنی
5. به محض `QA-PASS-RND-001` بریف `RND-002` را صادر کن (کالبدشکافی Alibaba) از روی صف
6. به محض `QA-PASS-TRD-001` بریف `TRD-002` را صادر کن

## خارج از دامنه

نوشتن رجیستری سایت. نوشتن نقشه منابع گمرک. رأی کیفیت.

## خودآزمایی

- بیش از یک CURRENT برای RND نساختم
- بسته را دور نریختم
- رویداد جعلی نساختم
